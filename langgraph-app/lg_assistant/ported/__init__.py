"""移植自 assistant-lite 的纯函数资产（无第三方依赖、无状态、不联网）。

- ``calc``   AST 安全算术求值（关键路径不走模型）
- ``grids``  黑白格逐格运算校验（图形推理的确定性校验）
- ``speech`` 屏幕长文 → 可朗读短句（不额外调模型）

这些模块与编排框架无关，**原样保留**以确保行为一致：它们是
「关键路径去模型化」的落地，重写等于把已验证的东西再赌一次。
"""

from . import calc, grids, speech

__all__ = ["calc", "grids", "speech"]
