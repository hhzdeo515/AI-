"""LangGraph 版本：眼镜端多场景助手（编排层）。

与同级目录的关系：

- ``../assistant-lite/``  自研编排层版本，**保留不动**，作为行为基线与对照。
- ``../dify-multiagent/`` Dify 场景 Agent 执行层，本图把它作为**可选执行后端**。

本包只做编排：把「设备事件 → 路由 → 场景执行 → 后处理」建模成显式状态图，
用 checkpointer 获得断点续跑能力（眼镜断连后从任意步骤恢复）。
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
