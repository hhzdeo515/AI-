# 2026-09-22 工作日志

## assistant-lite：三场景轻量级 multi-agent 助手（P0–P2 完成）

### 背景与决策
在 `E:\AI智能助手\` 下新建 `assistant-lite\`，替代旧 `dify-assistant\`（Dify Chatflow + Docker，太重）。
面向三场景：**会议纪要 / 拍照解题 / 锻炼指导**。砍掉旧的导航场景。

用户拍板的关键决策：
- 技术底座：**纯代码轻量 multi-agent**，不用 Dify、不用 Docker
- 模型：**阿里云百炼 OpenAI 兼容接口**（`dashscope.aliyuncs.com/compatible-mode/v1`），
  文本 `qwen-plus` / 视觉 `qwen-vl-max` / 语音 `qwen3-asr-flash`
- 交付：本地 CLI + 本地 Web 页（P5）
- 复用策略：搬旧项目的提示词与业务规则，**编排层全部重写**
- 锻炼场景定位：健康问卷建档 + 器械识别（照片/文字）+ **手动事件触发**的主动提醒/纠正 +
  结束总结与下一步计划。摄像头抽帧实时分析、视频离线分析**后置到第二版**
- 项目位置：新建目录；**git 仓库建在 `E:\AI智能助手\` 根目录**

### 架构核心思路
把"问答型"和"主动提醒型"统一成同一条 `event → orchestrator → agent → Reply` 事件流，
区别只在 Agent 内部有没有状态机。全项目只有 `Task` / `Reply` 两个数据结构。

### 完成情况（P0–P7 全部完成）
- **P0**：`config.py` / `schemas.py` / `llm.py` / `agents/base.py` / `run.py` CLI / 根 `.gitignore`
- **P1**：`session.py`（SQLite+WAL、owner 隔离、request_id 去重、资料归档）、
  `orchestrator.py`（五级路由）、`agents/meeting/`（状态机 48000 上限）、`agents/general.py`
- **P2**：`agents/exam/`（VISION→SOLVER→工具→REVIEWER→渲染）、
  `tools/grids.py`（复制旧 `check_grids`）、`tools/calc.py`（移植旧 `calculate`）
- **P3**：`agents/fitness/profile.py`（9 字段建档问卷）、`equipment.py`（15 项器械知识库）
- **P4**：`agents/fitness/state.py`（训练状态机 + 规则引擎）、`agent.py` 接入四个训练事件
- **P5**：`web/app.py` + `web/templates/index.html`；`llm.asr()` 走 `qwen3-asr-flash`
- **P6**：`tools/export.py`（md/txt/json/csv/docx/pdf）、`run.py export|resources`、`/api/export`
- **P7**：`agents/resource/`（资料查询 + 自然语言导出），补上会议纪要的导出闭环
- **P8**：接真实模型验证（用户提供百炼 Key）。修掉两个真问题：
  ①`exam` agent 没写 `_archive` 导致**题解从未落库**；
  ②会议纪要**编造原文没有的约束**（"2 个工作日内"），强化提示词后修复。
  另确立策略：**任何测试都不允许打真实 API**。
- **P9**：Web UI 重做为「AI 智能眼镜 Companion」。拆成 `templates/index.html` +
  `static/app.css` + `static/app.js`；新增 `progress.py`（请求级进度登记）与
  `/api/progress|profile|resource|reset`，让 pipeline 显示**真实执行阶段**而非假动画。
- **P9.1**：修 4 个真 bug（UI 锁死 / runComposer 缺 finally / `/api/chat` 丢弃 event /
  静态资源缓存），补上组间休息倒计时。
- **P10**：会议场景支持白板照片。修一个**功能回退**——旧版 dify 提示词是
  「根据 transcript 与白板文字生成纪要」，新版只认音频，图片被静默忽略。
  新增 `WHITEBOARD` 提示词（忠实抄录，不套考题结构），`_extract_transcript` 改为
  同时处理音频与图片，返回三元组；部分失败不再静默。
- **P11**：为眼镜接入做准备（用户问「接到眼镜还差什么」后，把不需要真机的三项全做了）
  1. **`speech` 播报字段**：旧版设计有 `display_text` + `speech_text`，迁移时丢了。
     现在 `Reply.speech` 与 `Reply.text` 分开。**关键：不额外调模型**——
     播报语由生成正文的那次调用顺带产出（结构化输出加字段 / 自由文本用 `<<<SPEECH>>>` 分隔），
     模型没给就规则压缩。新增 `speech.py`。
  2. **长音频分片**：按 `ASR_CHUNK_SECONDS`（默认 240s）切分。wav 用标准库，
     mp3/m4a 用 ffmpeg。缺 ffmpeg 不静默失败。新增 `tools/audio.py`。
  3. **异步任务**：`/api/chat/async` + `/api/task`，进程内线程池。新增 `tasks.py`。
- **P12**：TTS 与播报打断，补完「耳朵」链路（text → speech → audio → 打断控制）
  1. `llm.tts()` 走 dashscope **tts_v2（cosyvoice-v1）**。
     ⚠️ **sambert 那套在百炼新账号上返回空音频**（实测 `sambert-zhichun-v1` 返回 0 字节），不要用。
     `GET /api/speak` → `audio/mpeg`，带 `X-Playback-Generation` 响应头；
     超 `SPEECH_MAX_CHARS`(300) 直接 400。
  2. **播报打断**：session 里维护**播放代际号**，收到 `stop_playback` 事件 +1。
     服务端只做一半，设备端只需「比对代际号，不一致就丢弃并停播」。
     新增 `GET /api/playback` 供设备轮询；`ACTION_MAP` 补 `stop_playback`。
  3. Web UI 播报语旁加播放按钮（**事件委托**，渲染后无需重新绑定）。

测试：`tests/` 共 **160 项全通过**（Web 层新增 6 项）。
git 提交：… → `8e2ef33` P10 → `4616b87` P11 → `a8f48c6` P12

### 环境：外部安装物一律放 E 盘
- **ffmpeg 已装**：`E:\AI智能助手\tools\ffmpeg\bin\ffmpeg.exe`（79MB，6.1.1 静态构建）
- 来源：**GitHub 下载不通**（卡在 32KB），改用 **npmmirror 镜像**秒下
  `https://registry.npmmirror.com/-/binary/ffmpeg-static/b6.1.1/ffmpeg-win32-x64.gz`
- 路径经 `config.TOOLS_DIR` / `FFMPEG_PATH` 可覆盖；代码里不写死
- WorkBuddy 托管的 Python/Node 运行时固定在 C 盘，属环境决定，不动

### 未验证项
无。所有路径已用真实模型跑通（ASR 因无法生成人声，只验证了接口通路与分片逻辑）。

---

## 附带任务：为「光粒科技」定制简历

用户（齐正涵，AI 产品经理）要求结合飞书《AI产品经理简历撰写指南v1.2》写一份针对光粒科技的简历。

**产出**：`C:\Users\admin\Desktop\我的简历\齐正涵简历_光粒科技.{html,pdf}`（1 页 A4）

**关键信息**
- 光粒科技 = 杭州，2017 年成立，浙大光电系博士团队，自研 Holoresin 全息树脂光波导，
  产品是 **AR 全息智能泳镜 Holoswim 系列**（"水下 AI 教练"，实时数据 + 训练建议），
  2026 上半年销量同比 +400%，年内两轮 Pre-B
- 用户背景：罗曼智能 AI 产品经理（K12 智能批阅**一体机** = 软硬件一体）+ 腾讯 Agent 实习
- **匹配点**：assistant-lite 里的「锻炼指导」场景 + 「按眼镜形态设计」正好对口泳镜场景

**简历人设**：**懂硬件形态的 AI 产品经理**——把 assistant-lite 从"个人项目"重新框成
"设备形态如何决定 AI 交互设计"的产品论证（输出双通道 / 播报打断 / 异步 / 真实进度），
而不是讲代码量。

**技术要点（复用价值高）**
- PDF 太大（22MB）Read 工具读不了 → 用 `pypdf` 提取文字。**按约定装到 E 盘**：
  `pip install --target E:\AI智能助手\tools\pylibs pypdf`，脚本里 `sys.path.insert`
- 简历排版用 HTML + `@page` 打印 CSS，再用 headless Chrome `--print-to-pdf` 出 PDF
- **验证页数**：pypdf 读 PDF 页数 + 每页字符数，判断是否溢出（比肉眼看快）
- 预览排版：CDP `Emulation.setEmulatedMedia({media:"print"})` + A4 尺寸截图

### ⚠️ git 踩坑：`git add -A` 把安装物提交进了仓库
- 装 ffmpeg 到 `E:\AI智能助手\tools\` 后，下一次 `git add -A`（仓库根目录）**把 79MB 的
  ffmpeg.exe 一起提交了**，事后才发现——`.git` 涨到 30MB
- 修复：`.gitignore` 加 `tools/`，然后用 `git filter-branch --index-filter "git rm -r --cached --ignore-unmatch tools/"`
  重写全部 25 个提交，再 `reflog expire` + `gc --prune=now --aggressive`
- 结果：`.git` 从 30MB → **716K**，25 个提交与工作区文件全部完好
- **教训**：往仓库目录里装外部工具前，**先把它写进 `.gitignore`**。
  仓库根目录下的任何"工具/依赖/模型"目录都该先忽略再安装。

### 真实模型实测耗时（百炼 qwen-plus / qwen-vl-max）
会议纪要 ~10s ｜ 拍照解题五步链 ~27s ｜ 器械个性化 ~17s ｜ 训练总结 ~13s ｜
自然语言导出 <1s ｜ ASR 通路正常

### API Key
写在 `assistant-lite/.env`（已 gitignore 排除，确认未被跟踪）。轮换直接改该文件即可。

### 踩坑记录
1. **清华 PyPI 镜像没有 `dashscope` 包**（`from versions: none`），官方源有。
   装依赖要用 `-i https://pypi.org/simple`。
2. **`importlib.util.find_spec` 在父包不存在时抛 `ModuleNotFoundError` 而非返回 None**，
   用它做"模块是否存在"判断必须 try/except。
3. **粘性场景规则会吞掉其它场景的输入**：会议进行中，`计算 (18+24)*3` 被当成会议内容。
   修正：路由顺序必须是 事件 → 显式场景 → **关键词** → **粘性** → LLM。
   会议 collecting / 建档 awaiting / 训练 active 三种粘性状态都适用同一顺序。
4. **浮点精度**：`120/(1+0.2)` 结果是 `99.99999999999999`，断言要带容差。
5. **器械名不适合堆进关键词表**：改由 `FitnessAgent.can_handle` 查 `equipment.find()` 判定（0.9 分）。
6. **PDF 中文不必带字体文件**：reportlab 内置 `UnicodeCIDFont("STSong-Light")` 即可，
   避免把 9.7MB 的 simhei.ttf 塞进仓库。docx 中文则需设 `w:eastAsia` 字体。
7. **Git Bash 的 `/tmp` 路径在 Windows Python 里不存在**，脚本里写文件要用项目内相对路径。
8. **Web 层验收用 Flask `test_client` 比起真服务再 curl 更可靠**（10 项测试 1 秒跑完，无需端口）。
9. **同一句话可能同时命中两个场景的关键词**：「导出刚才的纪要成 Word」同时含 meeting 的
   「纪要」和 resource 的「导出」。解法：让**更具体的意图拿更高置信度**
   （导出意图 0.95 > 普通关键词 0.8），而不是靠 Agent 注册顺序。
10. **错误分支别复用带硬编码字段的构造函数**：`_list` 的空分支复用了硬编码
    `action="export"` 的 `_need`，导致「我有哪些资料」被标成 export 动作。
    单测没覆盖到（只断言了 scene 和 status），是**端到端跑 CLI 才发现的**——
    教训：每个分支的 `action` 也要断言。
11. **多 Agent 项目里，"归档"这类横切约定必须逐个 Agent 核对**：meeting / fitness 都写了
    `state_delta["_archive"]`，只有 exam 没写，于是**题解静默地从不落库**。
    编排层只认 `_archive`，Agent 自己塞的 artifact 不会触发持久化——
    这类"看起来返回了、其实没存"的 bug 不跑真实链路根本发现不了。
12. **测试绝不能依赖"没有 API Key"**：`test_full_workout_session_via_events` 原来断言
    模型降级文本，配上 Key 后立刻变红。需要模型的路径一律**打桩**，
    并**单独写一条降级路径测试**。否则测试结果会随环境漂移。
13. **提示词要显式禁止"合理补全"**：模型会把"需李四确认"扩写成"需在 2 个工作日内确认"，
    并凭空生成"启动财务流程"行动项。光说"不要虚构"不够，
    要举反例说明（"原文只说 X，就不能写成 Y"）才有效。
14. **两个测试文件里的同名 helper 容易写混**：`test_workout.py`/`test_exam_tools.py` 用
    `_fresh_db()`，其它用 `_fresh()`。复制粘贴测试时踩了两次。
15. **`[hidden]` 会被 CSS 的 `display:flex/inline-flex` 覆盖**：附件菜单、"End Session"、
    "Clear" 三个元素该隐藏却一直显示。必须显式写 `[hidden]{display:none!important}`，
    且要放在组件规则之前。
16. **SPA 视图切换必须监听 `hashchange` + `popstate`**：只用 `location.hash` 读初值、
    不监听变化的话，浏览器后退键失效；而且**同页改 hash 不会重载**，
    用 CDP/无头浏览器逐视图截图时全部拿到同一张图（我是靠对比 md5 发现的）。
17. **要"真实的进度动画"就得让后端上报进度**：前端假动画违背"每个动效都要有目的"。
    做法：`progress.py` 登记 request_id 的执行阶段，Agent 在关键步骤 `report()`，
    前端轮询 `/api/progress`。没有记录时返回空 steps，前端回退到"整体处理中"。
18. **Windows 上无法用 agent-browser 时的浏览器验证方案**（很好用，值得复用）：
    - headless Chrome 截图：**输出路径必须是纯英文**，中文路径下 Chrome 静默不写文件
    - 交互态验证：`--remote-debugging-port=9222` + Node 22 内置的 `WebSocket` 写个极简 CDP 客户端，
      可 `Page.navigate` / `Runtime.evaluate`（点击）/ `DOM.setFileInputFiles`（注入文件）/
      `Page.captureScreenshot` / `Emulation.setDeviceMetricsOverride`（断点）
    - **后台进程必须用 run_in_background 启动**：用 `&` 起的服务/Chrome 会随该条命令一起结束，
      后续 curl/截图拿到的是网络错误页
    - Git Bash 的 `/tmp` 在 Windows Python 里不存在，`curl -o /tmp/x` 会静默失败读到残留文件
19. **用 `innerHTML` 覆盖面板会摧毁后续需要的元素**：Fitness 的空闲态用 innerHTML 重写了整个
    Workout 面板，切到活跃态时 `#fit-sets` / `#fit-state` 已不存在 → `TypeError`。
    正确做法是**两种状态的 DOM 都放在页面里，只切换 `hidden`**，绝不重写 innerHTML。
20. **异步流程必须用 `try/finally` 释放锁**：上一条的 `TypeError` 从 `refreshState()` 抛出后，
    `runComposer` 的 `S.busy = false` 永远不会执行 → **发送按钮永久禁用，UI 锁死**。
    这类"一个渲染异常导致整个界面卡住"的 bug 只有真操作才暴露。
21. **前后端字段要对齐**：前端一直在提交 `event`（眼镜按键/语音事件的入口），
    但 `/api/chat` 只读 `text/scene`，整个字段被静默丢弃——事件驱动设计直接失效。
    前端发的每个字段都要在后端找到接收处。
22. **本地工具必须关掉静态资源缓存**（`SEND_FILE_MAX_AGE_DEFAULT = 0`），
    否则改了 `app.js` 用户还是拿旧版，排查时会严重误导（我就先误判成缓存问题，
    实际是另一个 bug，但缓存确实是真问题）。
23. **CDP/无头浏览器测试时，导航到完全相同的 URL 不会重载页面**。
    必须加时间戳参数（`/?t=Date.now()#view`），否则拿到的是上一次的 DOM，
    会看到"改了代码却没生效"的假象，并读到上一页残留的异常堆栈（行号对不上就是信号）。

### 未验证项
无。所有路径已用真实模型跑通（ASR 因无法生成人声，只验证了接口通路）。

### 下一步（若要继续）
首版有意不做的范围：摄像头抽帧实时分析、运动后视频离线分析、TTS、
多租户与鉴权、知识库语义检索、导出 SRT/ZIP 打包。
接续锚点在 `assistant-lite/README.md` 的「当前阶段 / 下一步」小节。

## 17:56 PRD 文档完成
- 为 assistant-lite 项目撰写完整 PRD，按用户模板含 5 大章节 + 附录
- 飞书文档：https://ocnli9bziyt3.feishu.cn/docx/RQX4dq42ZoMxkPxTjZlccApnnib
- 插入 9 张图（2 张架构图 + 7 张真实界面截图）
- 成本数据基于阿里云百炼公开定价，ROI 579%
- 本地备份：E:/AI智能助手/PRD_AI智能助手_眼镜端.md

## 18:10 PRD v2 修订完成
- 按用户给的 3 张参考图，重画：① Agent 架构图（中心 Agent + Tools/Memory/Planning/Action 四方向 + 三阶段调用说明）；② 产品研发全流程泳道图（6 泳道：业务/用户、产品、AI产品、UI/前端、研发/工程、测试、运营）
- 新增场景四「考试测评」：R011 错题归档 / R012 自动归因 / R013 掌握度评分 / R014 同类题推荐 / R015 测试报告
- 新增 test_quiz 测试模块 20 项（Q01-Q20），覆盖：归档、归因（4 类）、掌握度（单/多/零基础）、推荐（同类/跨知识点/去重）、报告（周/月/无数据）、边界（识别失败/重复/隐私）
- Bad case 从 5 条扩到 12 条，新增：多轮上下文断裂、长期记忆污染、语音打断竞态、错题归因错误、设备首次加载慢、模型降级失效、隐私泄露
- 新增迭代五「从拍照解题到考试测评」
- 创建 v2 飞书文档：https://ocnli9bziyt3.feishu.cn/docx/Hqc6drfqIoogGoxcdw1cAnOYnrg
- 本地备份：E:/AI智能助手/PRD_AI智能助手_眼镜端_v2.md

## 18:20 PRD 图片内联修订（原文档直改）
- 用户要求：图片直接插到对应章节位置，不创建新文档
- 方法：先 media-insert 上传图片拿到 file_token → 把 `![](https://feishu.cn/file/TOKEN)` 内联写进 markdown 对应章节 → `docs +update --command overwrite` 覆盖原文档
- 结果：9 张图分布到 3.1(1)/3.2(6)/3.3(1)/4(2)，不再堆在末尾
- 已删除冗余 v2 文档（Hqc6drfqIoogGoxcdw1cAnOYnrg）
- 唯一文档：https://ocnli9bziyt3.feishu.cn/docx/RQX4dq42ZoMxkPxTjZlccApnnib

## 18:35 架构图重画为三层分层
- 按用户要求改为 交互层 / 模型层 / 代码工程层 三层，每层模块严格对应真实代码：
  - 交互层：硬件入口 / Web 界面 / 语音交互 / 双通道输出
  - 模型层：qwen-plus / qwen-vl-max / qwen3-asr-flash / cosyvoice-v1 + 统一入口 llm.py
  - 工程层：编排路由 / 场景 Agent / 工具集 / 状态与记忆 / 语音处理 / 质量保障
- 调用关系：用户输入→工程层→模型层→工程层→交互层呈现（双向箭头标注）
- 上传新图覆盖原文档产品架构图章节，doc_id 不变

## 18:35 架构图重画为三层分层
- 按用户要求改为 交互层 / 模型层 / 代码工程层 三层，每层模块严格对应真实代码：
  - 交互层：硬件入口 / Web 界面 / 语音交互 / 双通道输出
  - 模型层：qwen-plus / qwen-vl-max / qwen3-asr-flash / cosyvoice-v1 + 统一入口 llm.py
  - 工程层：编排路由 / 场景 Agent / 工具集 / 状态与记忆 / 语音处理 / 质量保障
- 调用关系：用户输入→工程层→模型层→工程层→交互层呈现（双向箭头标注）
- 上传新图覆盖原文档产品架构图章节，doc_id 不变

## 18:50 业务流程图改为产品闭环
- 按用户给的"AI Young 数学批改流程"风格（输入/输出黄框，处理蓝框，持久化青框，两行 5+4 布局）
- 流程：拍照错题 → 图像与手写识别（VLM）→ 题目结构化与知识点匹配 → 解题·校验·TTS → 错题归档 → 更新能力矩阵 → 错因归因（4 类）→ 推荐后续练习 → 输出
- 替换文档原"产品研发全流程泳道图"章节，doc_id 不变

## 20:35 项目技术路线定位（架构归属确认）
- 讨论了「代码构建 vs 工作流构建 Agent」的区别，以及本项目是否属于 LangChain/LangGraph。
- **结论：assistant-lite 是零框架手写**，`requirements.txt` 只有 8 个包
  （openai/dashscope/flask/python-dotenv/pillow/requests/python-docx/reportlab），
  无任何 langchain / langgraph / llama-index / crewai，模型直连 OpenAI 兼容 SDK。
- **架构上对位的是 LangGraph 那一层（编排层），不是 LangChain（组件库）**。对照：
  StateGraph+条件边 ↔ ACTION_MAP+五级 route()；checkpointer ↔ session.py+state_delta；
  interrupt() ↔ need_input+待确认动作；astream_events ↔ progress.py+/api/progress；
  ToolNode ↔ tools/calc.py、grids.py。
- **框架有而我们没有的能力**（未来若需要再考虑引入）：状态快照链/时间旅行与任意点恢复、
  崩溃后 durable execution 续跑、子图嵌套与 supervisor/swarm 多智能体范式、Studio 可视化。
  当前异步任务是进程内线程池+内存表，重启即丢。
- **暂不引入框架的判断**：三场景、单机、单人规模下手写够用，重写路由/状态/进度三层是净亏损。
  触发重新评估的信号：设备断连后需从任意步骤恢复（眼镜场景）、Agent 间互相嵌套调用、
  长任务需崩溃恢复、团队接手需标准化。
- 对外介绍措辞建议：**「自研轻量级多 Agent 编排层（纯 Python，无第三方 Agent 框架）」**。

## 20:40 编排心智定位（补充上一条）
- 进一步厘清：**形态归代码侧，心智归 Dify 侧**。判据是「有没有环」。
- `exam` agent 是严格线性五步 `capture → recognize → solve → verify → render`，
  **没有"验证不过退回重解"的回边**（grep 确认无循环无重试）。
- `fitness` 跨轮次流转靠「每轮重新 route() + 粘性状态把自己送回同一 Agent」——
  是**用状态模拟环**，不是图上的环。
- 状态流动也是 Dify 式：编排层 load → Agent 改 → `state_delta` 回写，一次请求一个来回；
  不是 LangGraph 那种 state 在节点间流动 + reducer 合并。
- 比 Dify 强的地方：零 token 路由、嵌套状态、事件驱动、确定性工具、108 项不打 API 的测试。
  比 LangGraph 弱的地方：无图结构、无回退、无崩溃恢复、无时间旅行。
- **若要拿"图心智"不必引入框架**：把步骤建模成 `{"step":…, "next":…}` 显式转移表
  （沿用 ACTION_MAP 风格）即可保持零依赖。触发时机：出现「验证不通过自动重解」
  「纪要自检不合格重写」「建档问卷回改上一题」这类**回退需求**时。
