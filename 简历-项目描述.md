# 简历项目描述 · assistant-lite

> 面向 AI 智能眼镜的多场景轻量级 Agent 助手
> 本文档给三个篇幅版本，按你的简历排版选一个；后面是面试追问的应答准备。

---

## 技术栈（写一行即可）

```
Python 3.13 / 多模态 LLM（视觉·语音识别·语音合成）/ SQLite / Flask / 原生 JS
阿里云百炼 OpenAI 兼容接口 · qwen-plus · qwen-vl-max · qwen3-asr-flash · cosyvoice
```

---

## 版本 A · 精简版（一页简历，2 行）

**assistant-lite — AI 智能眼镜多场景助手**（个人项目）
纯 Python 实现的多 Agent 助手，覆盖会议纪要 / 拍照解题 / 锻炼指导三场景；
把原 Dify 工作流（30 节点）重构为可维护代码，去掉容器依赖，配套 160 项自动化测试。

---

## 版本 B · 标准版（推荐，直接粘贴）

**assistant-lite · 面向 AI 智能眼镜的多场景 Agent 助手**（个人项目）
`Python` `多模态 LLM` `SQLite` `Flask`

- **架构重构**：将原 Dify Chatflow 实现（30 节点）重构为纯 Python 多 Agent，
  移除 Docker + Dify 依赖，启动方式从「容器常驻」简化为一条命令；
  设计统一 `Task`/`Reply` 契约，把「问答型」与「主动提醒型」交互收敛到同一条事件流，
  新增场景只需实现两个方法（约 9,900 行，12 个迭代阶段）。
- **确定性优先**：关键路径尽量不走模型——算术用 AST 安全求值、图形推理用逐格运算校验、
  器械知识走内置知识库；模型只负责开放语义。据此做到 **160 项测试全部不依赖外部 API**，
  回归结果与环境无关。
- **多模态链路**：串联视觉理解、语音识别、语音合成三类模型，
  实现「会议录音/白板照片 → 纪要」与「题目照片 → 答案 + 程序校验」两条完整链路；
  实测纪要生成 ~10s、拍照解题 ~27s。
- **设备形态适配**：针对眼镜的听觉优先场景，拆分屏幕/语音双输出通道
  （播报语由生成正文的同一次调用顺带产出，不额外增加模型调用）；
  实现长音频分片、长耗时请求异步化（提交 0.06s 返回，轮询取结果）、
  以及基于代际号的播报打断机制。
- **工程质量**：SQLite + WAL 支持并发，按 owner 隔离数据，请求级幂等去重；
  开发过程中定位并修复多个隐蔽缺陷（结果未持久化、路由优先级冲突、前端状态锁死等），
  并为每类缺陷补充回归测试。

---

## 版本 C · 详细版（项目经历独立成块）

### assistant-lite · 面向 AI 智能眼镜的多场景 Agent 助手
**个人项目 ｜ Python / 多模态 LLM / SQLite / Flask**

**背景**：智能眼镜是听觉优先、算力受限的形态，但此前一版基于 Dify 的实现依赖
Docker 与低代码平台，改一处要重启容器，也无法针对设备形态做输出优化。

**我做了什么**

1. **把编排从低代码搬回代码**
   原实现是 30 节点的工作流。我重新设计了架构：统一的 `Task`/`Reply` 契约 +
   规则优先的路由层 + 按场景拆分的 Agent 包。关键设计是**把「用户提问」和
   「设备事件」统一成同一条事件流**——"生成会议纪要"和"第 3 组做完了"走同一条路径，
   区别只在 Agent 内部有没有状态机。这样不需要为「持续监测」另写一套机制。

2. **让确定性逻辑尽可能不走模型**
   算术求值、图形推理的逐格运算校验、器械知识检索都做成纯函数。
   好处有两个：更快更稳，以及**160 项测试可以在没有 API Key 的情况下全部跑通**——
   需要模型的路径一律打桩。这条约束后来帮我省了大量调试时间。

3. **多模态链路的工程化**
   拍照解题是「视觉精读 → 初解 → 程序校验 → 终审」四步链，
   初解与终审**各自重新查看原图**（而不是读初解的文字转述），避免转述丢信息。
   会议场景支持录音转写与白板照片识别，两者可混合。

4. **为设备形态提前做适配**
   网页能用不等于戴在脸上能用。我在没有真机的情况下先补齐了四件事：
   输出拆成屏幕/语音两路、长音频分片、长耗时请求异步化、
   以及用**代际号**实现播报打断（不试图取消已发出的请求，而是让旧音频作废）。

**结果**：约 9,900 行代码、160 项自动化测试、12 个迭代阶段全部交付；
纪要生成 ~10s、拍照解题 ~27s、语音合成 2.1s。

---

## 面试会被追问的 6 个点

### 1. 「为什么不用 LangChain / Dify？」
> 我先用 Dify 做过一版，能跑。但两个问题：改一个提示词要重启容器，迭代很慢；
> 而且它把编排藏在可视化节点里，做不了我要的路由优先级控制。
> 换成代码后，路由顺序、降级策略、状态机都是我显式写的，能测也能改。
> 不是说 Dify 不好——如果只是快速验证一个流程，它更合适。

**要点**：说清「什么场景下选什么」，而不是贬低工具。

### 2. 「你说多 Agent，Agent 之间怎么协作？」
> 不是 Agent 互相调用。所有 Agent 共享同一份输入输出契约，
> 由编排层按置信度选一个来执行，然后把结果写回会话状态。
> 这样做的好处是加新场景不用改老场景，也不会出现 Agent 之间互相调用导致的死循环。

**要点**：能讲清「不是自由对话式多 Agent」，避免被追问到底。

### 3. 「路由为什么要有优先级？」
> 踩过坑。我原本让「进行中的场景」优先，结果开会途中有人问"计算 (18+24)*3"，
> 被当成会议内容吞掉了。后来调整成：结构化事件 → 显式指定 → 关键词 → 进行中场景 → 模型兜底，
> 并补了回归测试。
> 另一个例子：「导出刚才的会议纪要」同时命中会议和资料两个场景的关键词，
> 我让更具体的意图（导出）拿更高置信度，而不是依赖注册顺序。

**要点**：这是最能体现「真的做过」的一段，务必讲具体。

### 4. 「拍照解题为什么不用 OCR？」
> OCR 会把图形推理题压平。比如九宫格题，OCR 只能输出一串字符，
> 行列位置、每格黑白、两图叠加关系全丢了，题就废了。
> 所以我直接用多模态模型读原图，而且初解和终审各读一遍。
> 如果是纯文字材料（合同、发票），OCR 反而更便宜更稳——这里不做是因为输入是图形题。

**要点**：展示「知道什么时候不该用某个技术」。

### 5. 「160 个测试不依赖 API，怎么做到的？」
> 需要模型的路径全部打桩。这不只是为了省钱——
> 早期有个测试断言的是模型降级时的文本，我配上 API Key 后它立刻变红了。
> 测试结果不该随环境变化，所以定成了硬约束。

### 6. 「这个项目上线了吗？有多少用户？」
> 这是我个人项目，没上线，数据是自测的。目前缺的是设备接入层——
> 摄像头取帧、麦克风音频流、按键事件产生这些要等真机 SDK。

**要点**：**大方承认是个人项目**。硬编用户数据被追问一句就崩，得不偿失。

---

## 不建议写的（会被打穿）

| 别写 | 原因 |
| --- | --- |
| 日活 / 用户数 / QPS | 个人项目没有，一问就露 |
| 「提升效率 60%」 | 没有对照实验，说不出怎么测的 |
| 高并发 / 分布式 / 微服务 | 是单机 SQLite，写这些等于给自己挖坑 |
| 「生产环境稳定运行」 | 没上生产 |
| 罗列一堆技术名词 | 「用了 Redis/Kafka/K8s」但讲不出为什么用，是减分项 |
| 「实现了多 Agent 协作框架」 | 容易让人期待过高，实际是编排层分发 |

---

## 按岗位调整侧重点

- **AI 应用 / Agent 方向**：把「统一事件流」「多模态链路」「路由优先级」放前面，
  突出对模型能力的边界判断（什么该给模型、什么该用代码）。
- **后端方向**：把「SQLite + WAL + owner 隔离 + 幂等去重」「异步任务」
  「分层架构与测试策略」放前面，弱化提示词部分。
- **全栈 / 前端方向**：补上 Web 部分——我按「智能眼镜 Companion」的方向重做了界面，
  三个场景给了三套视觉语言（波形 / 取景框 / 活动轨迹），做到不看文字能区分；
  另外**进度条显示的是后端上报的真实执行阶段，不是前端假动画**。
- **硬件 / 端侧方向**：重点讲「输出双通道」「音频分片」「异步化」「代际号打断」——
  这四条都是为了在没有真机时先把设备侧的接口准备好。

---

## 如果只保留一句话

> 把一套依赖低代码平台的 AI 助手，重写成约 9,900 行、160 项测试的纯代码多 Agent 系统，
> 并针对智能眼镜的听觉优先形态补齐了输出、时延与打断机制。
