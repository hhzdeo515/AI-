# C 档 / D 档 操作手册

> 配套 `C盘清理方案.html` 使用。A、B 档属于删除类操作，由我代劳或给你命令；C、D 档必须由你在系统设置和软件设置里操作，这份手册就是逐步指引。

---

## C 档 · 系统级调整（约 20.2 GB，需管理员）

### C-1　虚拟内存 —— ⚠️ 结论已修正，现在**不能**直接改小

**先看实测数据，这组数据推翻了我此前的建议**

| 指标 | 实测值 |
|---|---|
| 物理内存 | 31.7 GB |
| 页面文件分配 | 28,153 MB（27.5 GB） |
| 页面文件实际使用 | 7,093 MB（6.9 GB） |
| 页面文件峰值使用 | 7,117 MB（6.95 GB） |
| **提交内存 Commit Charge** | **54.33 GB** |
| **提交上限 Commit Limit** | **59.23 GB（已用 91.7%）** |
| 可用物理内存 | 3.75 GB |

**关键公式：提交上限 = 物理内存 + 页面文件 = 31.7 + 27.5 = 59.2 GB。**

你当前的提交内存是 54.33 GB，已经吃掉 91.7%。如果把页面文件砍到 8 GB，提交上限就降到 39.7 GB——**远低于当前的 54.33 GB，系统会立刻拒绝内存分配，程序会报「内存不足」或直接崩溃。**

所以：**在提交内存降下来之前，这 27.5 GB 页面文件是真实需要的，不能动。**我先前说「限制为 8 GB 释放 18.2 GB」是错的，请忽略。

**谁吃掉了这 54 GB（按提交内存排序，含进程实例数）**

| 进程 | 提交内存 | 实例数 | 说明 |
|---|---|---|---|
| **vmmem** | **15.90 GB** | 1 | WSL2 / Docker 虚拟机，**最大单项，也是页面文件膨胀的主因** |
| **python** | **6.94 GB** | 7 | 见下方明细 |
| WorkBuddyAI | 2.70 GB | 11 | |
| chrome | 2.25 GB | 19 | |
| 豆包 Doubao | 1.80 GB | 15 | |
| ChatGPT | 1.62 GB | 12 | |
| 飞书 Feishu | 1.47 GB | 13 | |
| node | 1.41 GB | 12 | |
| WorkBuddy | 1.34 GB | 7 | |
| ollama | 0.58 GB | 2 | 本地大模型服务 |
| mysqld | 0.57 GB | 1 | MySQL 服务 |
| Docker Desktop | 0.47 GB | 6 | |
| 微信 Weixin | 0.47 GB | 6 | |
| msedge | 0.42 GB | 8 | |

**python 明细（共 6.94 GB，值得单独看）**

```
4020 MB  pid=24172  python.exe -m xoscar.backends.indigen start_sub_pool -sn wnsm_...
1542 MB  pid=15744  python.exe -c "from multiprocessing.spawn import spawn_main..."
1528 MB  pid=21780  ...E:\deepseek harness\xinference-rerank\venv\Scripts\xinference...
```

这是一个 `deepseek harness / xinference-rerank` 推理服务 + xoscar 进程池。**如果你当前没在用它，停掉能一次释放约 6.9 GB 提交内存。**

**按收益排序的关闭清单**

| 优先级 | 关闭什么 | 释放提交内存 | 怎么做 |
|---|---|---|---|
| 1 | Docker Desktop + WSL | **约 15.9 GB** | 退出 Docker Desktop，然后 `wsl --shutdown` |
| 2 | deepseek harness / xinference 推理服务 | 约 6.9 GB | 停掉对应的 python 进程 |
| 3 | 多余的 Chrome 窗口（19 个实例） | 约 2.3 GB | 关掉不用的标签页和窗口 |
| 4 | Ollama（不用本地模型时） | 约 0.6 GB | 托盘退出 |
| 5 | 多余的 ChatGPT / 豆包 窗口 | 约 2 GB | 关掉闲置实例 |

**只要完成第 1、2 项，提交内存就能从 54 GB 降到约 31 GB，此时把页面文件设为固定 16 GB 就是安全的**，能再释放约 11.5 GB 磁盘。

**这也解释了 C 盘为什么一直在变小**：物理内存只剩 3.75 GB，系统在大量换页，Windows 的自动管理因此不断扩张页面文件。**磁盘问题和内存问题是同一个问题。**

**正确顺序：先降提交内存，再改页面文件**

1. 退出 Docker Desktop 并执行 `wsl --shutdown` → 释放约 15.9 GB 提交内存
2. 关掉多余的 Chrome 窗口、闲置的 python 进程
3. 等提交内存降到 35 GB 以下，再把页面文件设为**固定 16 GB**（提交上限 47.7 GB，留约 12 GB 余量）
4. 这样能释放约 11.5 GB 磁盘，且不会导致程序崩溃

**操作步骤（第 3 步之后再执行）**

1. `Win + R`，输入 `sysdm.cpl`，回车
2. 切到 **「高级」** 选项卡 → 「性能」区域点 **「设置」**
3. 再切到 **「高级」** → 「虚拟内存」区域点 **「更改」**
4. **取消勾选** 顶部的「自动管理所有驱动器的分页文件大小」
5. 选中 **C 盘** → 选择 **「自定义大小」**
6. 填入：**初始大小 8192 MB**，**最大值 8192 MB**
7. 点 **「设置」**，再点 **「确定」**
8. **重启电脑**才生效

**两个取舍点**

- 若你**不需要**分析蓝屏转储，8 GB 足够，直接按上面填。
- 若希望保留调试能力，可填 **16384 MB**，仍然释放约 10 GB，比 8 GB 更保险。
- 如果你的 C 盘上还有其他驱动器的分页文件，确认它们没被误删——**不要**把虚拟内存整个关掉，某些程序会因此崩溃。

### C-2　WinSxS 组件库清理（释放 2–4 GB）

**先看能清多少**（管理员身份打开 PowerShell）：

```powershell
DISM /Online /Cleanup-Image /AnalyzeComponentStore
```

输出里会有一行「组件存储清理建议：建议清理」。确认有收益后再执行：

```powershell
DISM /Online /Cleanup-Image /StartComponentCleanup /ResetBase
```

**注意**

- 这条命令耗时可能 10–30 分钟，中途别中断。
- 加了 `/ResetBase` 之后，**已安装的系统更新将无法卸载回滚**。如果你有回退某个更新的需求，去掉 `/ResetBase` 参数。
- 执行完可能需要重启。

---

## D 档 · 应用数据迁移到 D 盘（约 45.9 GB）

### 通用原则（先看这段，比具体步骤重要）

1. **必须用软件自带的设置项迁移，不要手工剪切目录**。手工移动会让软件找不到数据，轻则重新登录，重则聊天记录丢失。
2. **迁移前先退出该软件**（含托盘图标里的小图标）。
3. **迁移后先确认新位置数据完整，再删旧目录**。别急着删。
4. 目标盘建议选 **D 盘**（剩余 153 GB），迁移量最大的飞书也只需要 11.5 GB。
5. 迁移完成后，C 盘上对应目录应该只剩几百 KB 的配置残留，这是正常的。

### 各应用的迁移位置

| 应用 | 当前占用 | 设置入口 | 要改的项 |
|---|---|---|---|
| **飞书** | 11.48 GB | 设置 → 通用 | 「文件与缓存存储位置」改为 `D:\AppData\Lark` |
| **微信** | （含在腾讯 9.38 GB 内） | 设置 → 文件管理 | 「文件保存位置」改为 `D:\AppData\WeChat` |
| **企业微信** | （含在腾讯 9.38 GB 内） | 设置 → 常规 | 「文件存储位置」改为 `D:\AppData\WXWork` |
| **WPS** | 7.04 GB | 配置和修复工具 → 高级，或 设置 → 备份中心 | 缓存路径、备份路径均改到 `D:\AppData\WPS` |
| **豆包** | 3.98 GB | 设置 → 通用 | 存储/缓存位置改为 `D:\AppData\Doubao` |
| **TRAE SOLO** | 3.67 GB | 设置 → 通用 | 数据目录改为 `D:\AppData\TRAE` |
| **百度网盘** | 2.56 GB | 设置 → 传输 | 下载目录改到 D 盘（缓存目录不可改，只能定期清） |

> 各版本菜单名称可能略有差异，以你机器上实际的为准。找不到对应选项时告诉我，我帮你在配置文件里定位。

### 迁移前先建好目标目录

在 D 盘建一个统一目录，方便管理：

```
D:\AppData\
  ├── Lark\
  ├── WeChat\
  ├── WXWork\
  ├── WPS\
  ├── Doubao\
  ├── TRAE\
  └── Baidu\
```

### 飞书单独说明（最大单项，结论已修正）

飞书总共 11.48 GB。深挖后**推翻了之前的判断**——原先认为「几乎全是真实数据」，实际不是：

| 目录 | 大小 | 性质 | 处理方式 |
|---|---|---|---|
| `profile_explorer\Service Worker` | **6.3 GB** | 内嵌浏览器缓存 | **直接清除**，无需迁移 |
| `profile_explorer\Cache` | 264 MB | 缓存 | 直接清除 |
| `profile_explorer\Code Cache` | 113 MB | 缓存 | 直接清除 |
| `profile_explorer\IndexedDB` | 20 MB | 网站本地数据 | 清除后部分网页要重新登录 |
| `doubao` | 880 MB | 豆包组件 | 迁移 |
| `profile_main` | 306 MB | 主配置 | 迁移 |
| `sdk_storage\log` | 510 MB | 运行日志 | 直接删除 |
| `iron\users` | 2.32 GB | 另一套内嵌浏览器运行时 | 迁移 |

**所以正确的顺序是：**

1. **先清缓存（约 6.7 GB）** —— 完全退出飞书后，删除 `profile_explorer\Service Worker`、`Cache`、`Code Cache` 三个目录。飞书下次启动会自动重建，聊天记录不受影响。零风险、零配置。
2. **再迁移剩余部分（约 4.8 GB）** —— 如果还想进一步腾空间，把整个 `LarkShell` 搬到 F 盘。

**为什么用目录联接（junction），而不是设置项**

飞书的 `Preferences` 文件里硬编码了 `C:\Users\admin\AppData\Roaming\LarkShell\aha\users\e70aa...`，官方设置只能改「文件下载位置」，改不了数据目录本身。所以要用 junction：

```cmd
:: 1. 完全退出飞书（任务管理器确认没有 Feishu.exe 残留进程）
:: 2. 移动到 F 盘
robocopy "C:\Users\admin\AppData\Roaming\LarkShell" "F:\AppData\LarkShell" /E /MOVE /R:1 /W:1
:: 3. 建立联接
mklink /J "C:\Users\admin\AppData\Roaming\LarkShell" "F:\AppData\LarkShell"
```

junction **不需要管理员权限**（符号链接才需要）。做完之后飞书仍以为数据在 C 盘，实际落在 F 盘。

**顺带一提**：飞书自带的 `disk_cleanup_config` 会自动清理日志、seed 和 7 天前的文件/图片/媒体，但**不清理 `profile_explorer` 的 Service Worker**——这就是它能长到 6.3 GB 的原因。

---

## 执行顺序建议

```
1. A 档 + B 档（我代劳或给你命令）        → 释放 22.4 GB，C 盘剩余约 41.8 GB
2. D 档迁移（你在软件设置里操作）          → 释放 45.9 GB，C 盘剩余约 87.7 GB
3. C 档系统调整（需管理员 + 重启）         → 释放 20.2 GB，C 盘剩余约 108 GB
```

把 C 档放最后，是因为它要重启；D 档放中间，是因为迁移需要逐个软件操作，比较费时间但风险低。

---

## 需要我做的部分

A 档和 B 档里的删除类操作，只要你说一声范围，我会：

1. 先生成文件清单快照存到工作区，方便事后核对；
2. 按每批不超过 10 项分批执行，做完一批核对一次；
3. 纯缓存用官方命令永久删除（这样才真正释放空间），安装包与旧版本走回收站；
4. 遇到需要管理员权限的项目，单独列出来给你命令。

**当前状态：尚未执行任何删除操作。**
